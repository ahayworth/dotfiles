interface:
  display_name: "Resume Plan"
  short_description: "Resume work from repo plans."
  default_prompt: "Use $resume-plan to reconstruct current status from plans and continue safely."
policy:
  allow_implicit_invocation: false
