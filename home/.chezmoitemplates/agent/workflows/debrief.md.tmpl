1. Compare what we set out to do against what actually got built. Use this session's plan-mode plan file as the reference when one exists (e.g. Claude writes one under `~/.claude/plans/`); otherwise use what we discussed in the conversation. Review the diff, tests, and any TODOs before summarizing.
2. Report four buckets clearly:
   - completed as intended
   - completed differently
   - deferred or omitted
   - follow-up work that surfaced during implementation
3. Call out missing verification and residual risks, and flag anything that still needs a decision.
