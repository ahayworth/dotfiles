config:
  dotpath: ../../dotfiles
dotfiles:
  gitconfig:
    dst: ~/.gitconfig
    src: git/gitconfig
  gitconfig.work:
    dst: ~/.gitconfig.work
    src: git/gitconfig.work
profiles:
  git:
    dotfiles:
      - gitconfig
  git.work:
    include: 
      - git
    dotfiles:
      - gitconfig.work
